using brute force method 
find all the sub array sum 

code:-> 
        count = 0;
        for(int i=0;i<n;i++){
          int sum = 0;
          for(int j=i;j<n;j++){
            if(sum == k) count++;
            sum+=nums[j];
          }
        }
